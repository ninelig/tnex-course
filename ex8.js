const mysticalOccurrences = [
    'Dormammu Attack',
    'Multiverse Glitch',
    'Ancient One’s Warning',
    'Time Loop',
    'Portal Opening'
  ];
  

  const lastThreeEvents = mysticalOccurrences.slice(-3);
  
  console.log(lastThreeEvents);